// Semelhante ao while, porém amplamente utilizado

// É a estrutura de repetição mais fácil de compreender, tente sempre preferir por ela.

// Com ela tudo pode ser declarada em uma linha, dentro do for

for ( let i = 0; i < 100; i += 3 ) {
    console.log( `A soma de i com 2 é: ${i + 2}` );
}