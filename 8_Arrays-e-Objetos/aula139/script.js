// Uma técnica muito utilizada na programação é o loop nos arrays

// Como é uma lista com muitos valores, muitas vezes precisamos ver cada um deles

let nomes = ["Fernando", "Maria", "João"];

for ( let i = 0; i <= nomes.length; i++ ) {
   console.log(nomes[i]);
}